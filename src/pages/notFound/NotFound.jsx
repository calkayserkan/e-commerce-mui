import { Box } from "@mui/joy";

function NotFound() {
  return (
    <Box
      sx={{
        width: "100%",
        height: "90vh",
        display: "flex",
        flexDirection: "row",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      Not Found
    </Box>
  );
}

export default NotFound;
